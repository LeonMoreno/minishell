#include <stdio.h>

int ft_ft(void)
{
	int	x;

	x = 7;
	if (x == 7)
		return (0);
	else
		return (1);
	return (7);
}

int main(void)
{
	int re;

	if (re = ft_ft())
		printf("Return if %d\n", re);
	printf("Return fuerra %d\n", re);
	
	return (0);
}
